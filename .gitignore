# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Environment
.env*.local

# Editor / OS
.DS_Store
*.log
.vercel
next-env.d.ts.bak

# Local data / scratch
scripts/data/

# Large source videos (uploaded to Cloudinary, not committed).
# Scoped to the repo root so served assets in public/video/ stay tracked.
/*.mp4
/*.MP4
/*.mov
/*.MOV

# Local design sources — uploaded to R2, not served from the repo.
/*.png
!/public/**/*.png
!/src/**/*.png
