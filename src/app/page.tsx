import HeroVideo from '@/components/HeroVideo';

/**
 * Home page.
 *
 * A single full-screen hero section with an autoplaying background video.
 * Everything else (the parallax storytelling deck and the closing band)
 * has been removed — the page is just the video hero now.
 */
export default function HomePage() {
  return <HeroVideo />;
}
